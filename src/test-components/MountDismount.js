import React from 'react'
import './MountDismount.css'

const MountDismount = ({ isShowing }) => {
    show ? <div style={{ animation: `fadeIn 1s` }}>HELLO</div> : null
}

export default MountDismount
